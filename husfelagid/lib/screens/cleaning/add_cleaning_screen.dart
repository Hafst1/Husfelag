import 'package:flutter/material.dart';

class AddCleaningScreen extends StatelessWidget {
  static const routeName = '/add-cleaning';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bæta við þrif"),
      ),
    );
  }
}