import 'package:flutter/material.dart';

class CreateAssociationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Búa til húsfélag'),
      ),
      body: Container(),
    );
  }
}
