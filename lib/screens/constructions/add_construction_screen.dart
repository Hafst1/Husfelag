import 'package:flutter/material.dart';

class AddConstructionScreen extends StatelessWidget {
  static const routeName = '/add-construction';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bæta við framkvæmd"),
      ),
    );
  }
}